[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "ugc-video-harness"
version = "0.3.0"
description = "Beat-first UGC content, voice, evidence, and visual planning harness"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.28.0",
  "openai>=1.68.0",
  "pydantic>=2.10.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.3.0"]

[project.scripts]
ugc-harness = "ugc_harness.stage_one.cli:main"
ugc-voice = "ugc_harness.stage_two.cli:main"
ugc-evidence-plan = "ugc_harness.stage_three.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
